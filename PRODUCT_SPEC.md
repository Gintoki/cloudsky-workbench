# Product Specification

## 1. 产品定义

**CloudSky Capital & Strategy Workbench** 是云天畅想投融资与战略团队的内部事实、指标、研究、估值和知识资产工作台。它的第一目标不是“展示信息”，而是让每个关键结论都能回答：

1. 这是什么数据或观点？
2. 来源在哪里，采用什么统计口径和期间？
3. 谁创建、修改和审核？
4. 当前是否有效，历史版本是什么？
5. 哪些外部回复或资本市场叙事依赖它？

初期用户 4—5 人，桌面端优先，移动端用于基本查看和审核。

## 2. 用户与权限

### 2.1 角色

| 角色 | 核心权限 |
|---|---|
| Administrator | 用户、角色、权限、数据源与系统配置；全部业务权限 |
| Director | 查看和编辑全部业务数据；审核事实、指标、叙事和正式回复 |
| Analyst | 创建、编辑、分析和提交审核；不得自行批准重要内容 |
| Viewer | 仅查看 Approved 且未过期、未软删除的内容 |

### 2.2 权限粒度

权限采用 `resource.action` 命名，例如：

- `facts.read`, `facts.create`, `facts.update`, `facts.submit`, `facts.approve`
- `metrics.read`, `metrics.create`, `metrics.update`, `metrics.approve`
- `audit.read`
- `users.manage`, `roles.manage`, `settings.manage`

所有路由处理器和服务端操作先认证、再授权、后查询。查询还需应用行级规则，例如 Viewer 自动过滤非 Approved 内容。

## 3. Phase 1 范围

### 3.1 登录与账号

- 邮箱、密码登录；
- HttpOnly、Secure（生产）、SameSite=Lax 会话 Cookie；
- 密码采用强 KDF 哈希，不保存明文；
- 登录成功、失败和退出记录；
- 修改密码、邀请、启停用户的数据模型和服务接口；
- 登录节流接口预留；
- Demo Provider 仅在显式环境变量下启用。

### 3.2 首页

首页只展示可行动信息：

- 待审核事实和指标；
- 最新 Approved 经营指标；
- 近期需回复事项；
- 正在进行的投融资/战略事项；
- 最近更新和审计记录；
- 行业动态、Watchlist 作为后续模块的明确空状态；
- 快速进入事实库、指标库和审计日志。

### 3.3 公司事实库

首期实现：

- 列表、搜索、分类和状态筛选；
- 事实详情；
- 新建、编辑、提交审核、审核；
- 来源、原文引用、期间、口径、责任人与有效期；
- 每次业务修改生成不可变 `fact_versions`；
- 软删除；
- Viewer 仅看有效 Approved 版本；
- 空、加载、错误和无权限状态。

状态机：

```text
Draft -> Pending Review -> Approved -> Superseded
  |            |              |
  +----------> Archived <-----+
```

Analyst 可进行 `Draft -> Pending Review`；Director/Administrator 可进行审核。批准新版本时，旧的 Approved 版本变为 Superseded。

### 3.4 指标库

首期实现：

- 指标定义与口径；
- Actual / Budget / Forecast；
- Bear / Base / Bull；
- 月度、季度、年度；
- 结构化期间、单位、币种、来源与状态；
- 同比、环比计算；
- 表格、趋势图和版本历史；
- CSV 导入导出接口；
- 异常值和重复期间校验。

首期图表仅展示实际存在的示例记录，且明确标注 Demo。

### 3.5 审计日志

记录：

- 登录、退出、登录失败；
- 新建、修改、提交审核、批准、归档、软删除；
- 用户和权限变更；
- 敏感文件访问（后续文件模块启用）；
- Actor、动作、资源、资源 ID、时间、IP、User-Agent、变更摘要和请求关联 ID。

审计日志追加写入，业务角色不能修改或删除。

## 4. 后续产品模块

### Phase 2：市场与情报

- Companies / Securities / Watchlist；
- Mock、CSV/Excel、Manual 三类行情适配器；
- RSS 和公开接口的行业动态采集；
- 去重、分类、实体识别、摘要、重要性评分、重试和采集日志；
- Wind 仅预留适配器，不假设字段、SDK、授权范围或密钥。

已交付的行业动态切片：读取 Notion `01 Projects → 行业雷达` 的现有内容，结构化保存行业分类、公司/主题关联、事件时间线、摘要与来源，并提供授权后的分类筛选、公司筛选、时间排序和详情查看。持续同步、复杂 Dashboard、AI Agent 和高级分析不在本切片范围。

### Phase 3：估值

- 可比公司、PS、DCF 和 Bear/Base/Bull；
- 输入项与公式项分离；
- 公式、勾稽、异常校验、版本、复制和 Excel 导出；
- 敏感性分析矩阵；
- Python 计算服务以确定性结果为主，前端不作为唯一计算源。

### Phase 4：叙事与 Investor Q&A

- 叙事版本、事实与指标证据；
- 两版本差异；
- 过期数字、口径冲突、无支撑表述和跨材料不一致提示；
- 历史问题搜索；
- 内部判断与正式外部口径严格分离。

### Phase 5：AI 问答

- 仅检索当前用户有权访问的文档和数据；
- Approved 内容优先；
- 回答拆分为事实、推断、建议；
- 逐条引用；
- 冲突提示；
- 资料不足时明确拒绝推测；
- 草稿永远不会自动成为正式口径。

## 5. 非功能要求

### 安全

- 服务端 RBAC 与行级过滤；
- Zod 输入验证；
- ORM 参数化查询；
- Cookie 会话轮换和过期；
- 文件下载使用授权后的短时地址；
- AI 检索前应用 ACL，检索后再次过滤；
- 所有密钥只来自环境变量；
- 生产禁止 Demo Provider 和弱默认密钥。

### 可用性

- 桌面端高信息密度；
- 1280px 以上优先，窄屏保留查看能力；
- 全局搜索、面包屑、筛选、排序、保存视图和批量操作的统一交互规范；
- 所有列表包含空、加载、错误、无权限状态；
- 关键表单支持键盘操作和清晰错误信息。

### 性能目标

- 常用列表在 10 万条以内使用服务端分页与索引；
- 首页聚合请求 P95 小于 800ms（不含第三方接口）；
- 写操作带请求关联 ID，审计写入与业务事务一致；
- 外部数据采集和 AI 任务异步执行。

## 6. 明确假设

- 首期不接真实云天畅想内部数据；
- 首期不接 Wind，不虚构接口；
- 首期采用内置 Credentials Provider，接口保持可替换；
- 首期以单组织为边界，但表中保留 `organization_id`，避免未来数据隔离重构；
- 生产部署平台、域名、邮件发送服务和对象存储尚未确定；
- FastAPI 首期只搭建健康检查与接口契约，不承担 Web 主业务鉴权。

## 7. 成功标准

Phase 1 验收时，四种角色能通过独立账号登录；服务端能阻止越权；事实和指标可创建、修改、送审和审核；Viewer 只能读取 Approved 内容；每次变更均可在版本与审计记录中追踪；全套开发环境能由 Docker Compose 启动。
